# Copy to .env.local (git-ignored) for local dev; set the same vars via EAS env for builds.
# Both are PUBLIC by design (anon key) — never put the service_role key or ANTHROPIC_API_KEY here;
# those live only as Supabase secrets. See docs/backend-push.md.

EXPO_PUBLIC_SUPABASE_URL=https://vldepqrkbdrspgtbyyxu.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
